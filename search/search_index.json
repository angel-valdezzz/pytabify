{"config":{"lang":["es"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"pytabify","text":"<p>Convierte datos tabulares entre <code>CSV</code>, <code>JSON</code> y <code>XLSX</code> con una API Python corta y un wrapper oficial para <code>Robot Framework</code>.</p> <p>Instalacion Inicio rapido Referencia</p> <ul> <li> <p> Instala y valida</p> <p>Entra con <code>pip</code> o <code>poetry</code> y confirma en segundos que la libreria carga bien.</p> <p>Ver instalacion</p> </li> <li> <p> Empieza rapido</p> <p>Carga un archivo, modifica una celda y exporta el resultado sin navegar por toda la API.</p> <p>Ver quickstart</p> </li> <li> <p> Usa ejemplos reales</p> <p>La documentacion parte de flujos cubiertos por pruebas: <code>JSON -&gt; CSV</code>, <code>XLSX -&gt; JSON</code> y Robot Framework.</p> <p>Ver ejemplos</p> </li> <li> <p> Resuelve errores comunes</p> <p>Errores de <code>sheet_name</code>, extensiones no soportadas, rutas invalidas o datos no rectangulares.</p> <p>Ir a troubleshooting</p> </li> </ul> <p>Enfoque actual del proyecto</p> <p><code>pytabify</code> hoy es una libreria Python con adaptador oficial para Robot Framework. Si en el futuro aparece un CLI propio, se puede documentar como una entrada adicional sin romper esta estructura.</p>"},{"location":"#introduccion","title":"Introduccion","text":"<p><code>pytabify</code> esta orientado a equipos tecnicos que necesitan cargar, validar, mutar y persistir datos tabulares sin construir conversiones manuales para cada formato.</p> <p>Va dirigido principalmente a:</p> <ul> <li> <p>Desarrolladores Python</p> <p>Para scripts, fixtures, transformaciones simples y automatizacion de datos tabulares.</p> </li> <li> <p>Equipos de QA</p> <p>Para pruebas automatizadas donde los datos viven en archivos o deben moverse entre formatos.</p> </li> <li> <p>Usuarios de Robot Framework</p> <p>Para consumir tablas con acceso estable por fila, columna y adaptadores listos para keywords.</p> </li> </ul>"},{"location":"#que-problema-resuelve","title":"Que problema resuelve","text":"<p>Trabajar con datos tabulares pequenos o medianos suele degradarse rapido en codigo repetitivo:</p> <ul> <li>leer <code>CSV</code>, <code>JSON</code> y <code>XLSX</code> con librerias distintas;</li> <li>normalizar columnas y orden de esquema manualmente;</li> <li>mutar filas sin romper consistencia entre registros;</li> <li>exportar otra vez a un formato distinto;</li> <li>repetir ese trabajo en Python y despues volver a modelarlo para Robot Framework.</li> </ul> <p><code>pytabify</code> encapsula ese flujo en un contrato tabular unico. La herramienta carga los registros, mantiene un esquema estable, permite enriquecer la tabla sin perder sincronizacion entre filas y la vuelve a persistir en el formato de salida que necesites.</p> <p>Cuando encaja bien</p> <p>Encaja mejor cuando el problema real es mover y ajustar datos tabulares con poco codigo, no construir un pipeline de datos complejo ni un motor ETL.</p>"},{"location":"#lo-que-puedes-hacer","title":"Lo que puedes hacer","text":"PythonRobot Framework Leer, mutar y exportar<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_file(\"people.json\")\ndatatable[0][\"country\"] = \"MX\"\n\nDataTableSaver.into_csv(datatable, \"people.csv\")\n</code></pre> Crear una tabla y guardar a JSON<pre><code>*** Settings ***\nLibrary    pytabify.robot.PyTabifyLibrary    WITH NAME    PyTabify\n\n*** Test Cases ***\nGuardar tabla enriquecida\n    ${records}=    Create List\n    ...    ${{ {\"name\": \"Alice\", \"age\": 30} }}\n    ...    ${{ {\"name\": \"Bob\", \"age\": 25} }}\n    ${table}=    PyTabify.Create Data Table From Records    ${records}\n    ${table}=    PyTabify.Set Data Table Value    ${table}    0    country    MX\n    PyTabify.Save Data Table To Json    ${table}    people.json\n</code></pre>"},{"location":"#flujo-recomendado","title":"Flujo recomendado","text":"<ol> <li>Instala la libreria.</li> <li>Ejecuta el quickstart completo.</li> <li>Salta a <code>Python</code> o <code>Robot Framework</code> segun tu caso.</li> <li>Usa la referencia solo cuando ya sepas el flujo que quieres resolver.</li> </ol>"},{"location":"#mapa-rapido","title":"Mapa rapido","text":"<ul> <li> <p>Instalacion</p> <p>Prepara el entorno y valida que la libreria carga.</p> <p>Abrir instalacion</p> </li> <li> <p>Inicio rapido</p> <p>Sigue un flujo completo con input, ejecucion y output esperado.</p> <p>Abrir quickstart</p> </li> <li> <p>Referencia</p> <p>Consulta <code>DataTableCreator</code>, <code>DataTableSaver</code> y <code>DataTable</code> sin recorrer toda la guia.</p> <p>Abrir referencia</p> </li> <li> <p>Interna</p> <p>Revisa arquitectura y extension solo si mantienes la herramienta.</p> <p>Abrir arquitectura</p> </li> </ul> Por que la referencia no va primero <p>La API publica de <code>pytabify</code> es pequena. Para este tipo de herramienta, una referencia larga al inicio agrega friccion. El orden elegido privilegia uso real primero y detalle despues.</p>"},{"location":"#cobertura-de-formatos","title":"Cobertura de formatos","text":"Formato Lectura Escritura Nota practica <code>CSV</code> Si Si Los valores se leen como texto. <code>JSON</code> Si Si Conserva bien tipos nativos simples. <code>XLSX</code> Si Si Requiere <code>sheet_name</code> al leer. <p>Regla de oro</p> <p>Si necesitas preservar <code>int</code>, <code>bool</code> y <code>None</code> durante el round-trip, prefiere <code>JSON</code> o <code>XLSX</code>. <code>CSV</code> es el formato mas interoperable, pero no conserva tipos de la misma forma.</p>"},{"location":"examples/python/","title":"Ejemplos en Python","text":"<p>Estos flujos reflejan escenarios ya cubiertos por las pruebas del proyecto.</p> <p>Inicio rapido Guardar tablas</p>"},{"location":"examples/python/#convertir-json-a-csv","title":"Convertir JSON a CSV","text":"Round-trip simple desde archivo<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_file(\"people.json\")\nDataTableSaver.into_csv(datatable, \"people.csv\")\n</code></pre> <p>Comportamiento esperado</p> <p>Este flujo esta cubierto por los tests end-to-end del proyecto. Es el mejor ejemplo base para usuarios que solo quieren transformar un archivo rapidamente.</p>"},{"location":"examples/python/#cargar-xlsx-y-persistir-como-json","title":"Cargar XLSX y persistir como JSON","text":"XLSX -&gt; JSON<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_file(\"people.xlsx\", sheet_name=\"People\")\nDataTableSaver.into_json(datatable, \"people.json\")\n</code></pre> Si falta <code>sheet_name</code> <p>La lectura de <code>XLSX</code> lanza una excepcion cuando no recibe un nombre de hoja. Si no conoces la hoja correcta, revisa primero el archivo fuente.</p>"},{"location":"examples/python/#enriquecer-registros-antes-de-guardar","title":"Enriquecer registros antes de guardar","text":"Expandir el esquema y exportar<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_records(\n    [\n        {\"name\": \"Alice\", \"age\": 30},\n        {\"name\": \"Bob\", \"age\": 25},\n    ]\n)\n\ndatatable[0][\"country\"] = \"MX\"\n\nDataTableSaver.into_json(datatable, \"people-enriched.json\")\n</code></pre> AntesDespues Registros iniciales<pre><code>[\n    {\"name\": \"Alice\", \"age\": 30},\n    {\"name\": \"Bob\", \"age\": 25},\n]\n</code></pre> Salida serializada<pre><code>[\n    {\"name\": \"Alice\", \"age\": 30, \"country\": \"MX\"},\n    {\"name\": \"Bob\", \"age\": 25, \"country\": None},\n]\n</code></pre>"},{"location":"examples/python/#usarlo-en-pruebas","title":"Usarlo en pruebas","text":"Fixtures y assertions<pre><code>from pytabify import DataTableCreator\n\ndatatable = DataTableCreator.from_records(\n    [\n        {\"name\": \"Alice\", \"active\": True, \"nickname\": None},\n        {\"name\": \"Bob\", \"active\": False, \"nickname\": None},\n    ]\n)\n\nassert datatable[0].active.value is True\nassert datatable[1].nickname.value is None\n</code></pre> <p>Eleccion de formato</p> <p>Si las assertions dependen de tipos nativos, usa <code>from_records</code>, <code>JSON</code> o <code>XLSX</code>. Evita basarte en tipos al leer desde <code>CSV</code>.</p>"},{"location":"examples/robot-framework/","title":"Ejemplos en Robot Framework","text":"<p><code>pytabify</code> expone un wrapper oficial para Robot Framework mediante <code>PyTabifyLibrary</code>.</p> <p>Inicio rapido Arquitectura</p>"},{"location":"examples/robot-framework/#configuracion-minima","title":"Configuracion minima","text":"Importar la libreria<pre><code>*** Settings ***\nLibrary    pytabify.robot.PyTabifyLibrary    WITH NAME    PyTabify\n</code></pre>"},{"location":"examples/robot-framework/#crear-una-tabla-desde-registros","title":"Crear una tabla desde registros","text":"RobotDataTable desde memoria<pre><code>*** Test Cases ***\nCrear tabla desde registros\n    ${records}=    Create List\n    ...    ${{ {\"name\": \"Alice\", \"age\": 30} }}\n    ...    ${{ {\"name\": \"Bob\", \"age\": 25} }}\n    ${table}=    PyTabify.Create Data Table From Records    ${records}\n    ${headers}=    PyTabify.Get Data Table Headers    ${table}\n    Should Be Equal    ${headers}    ${[\"name\", \"age\"]}\n</code></pre>"},{"location":"examples/robot-framework/#leer-una-fila-con-acceso-dual","title":"Leer una fila con acceso dual","text":"Acceso por atributo y por llave<pre><code>*** Test Cases ***\nInspeccionar fila\n    ${records}=    Create List\n    ...    ${{ {\"name\": \"Alice\", \"age\": 30} }}\n    ${table}=    PyTabify.Create Data Table From Records    ${records}\n    ${row}=    PyTabify.Get Data Table Row    ${table}    0\n    Should Be Equal As Strings    ${row.name}    Alice\n    Should Be Equal As Integers    ${row}[age]    30\n</code></pre>"},{"location":"examples/robot-framework/#mutar-y-guardar-la-tabla","title":"Mutar y guardar la tabla","text":"Agregar columna y persistir a JSON<pre><code>*** Test Cases ***\nMutar tabla y guardar\n    ${records}=    Create List\n    ...    ${{ {\"name\": \"Alice\", \"age\": 30} }}\n    ...    ${{ {\"name\": \"Bob\", \"age\": 25} }}\n    ${table}=    PyTabify.Create Data Table From Records    ${records}\n    ${table}=    PyTabify.Set Data Table Value    ${table}    0    country    MX\n    PyTabify.Save Data Table To Json    ${table}    people.json\n</code></pre> Lo que expone RobotCuando conviene usarlo <ul> <li><code>RobotDataTable</code> itera filas adaptadas.</li> <li><code>RobotDataRow</code> permite acceso por atributo y por llave.</li> <li><code>Get Data Table Headers</code> acepta tanto <code>RobotDataTable</code> como <code>DataTable</code>.</li> </ul> <ul> <li>Cuando tus pruebas viven en Robot y no quieres mapear manualmente filas.</li> <li>Cuando necesitas un contrato tabular estable entre keywords.</li> </ul> <p>Compatibilidad</p> <p>El wrapper acepta tanto <code>RobotDataTable</code> como <code>DataTable</code> nativo en varias operaciones, lo que simplifica flujos mixtos entre Python y Robot.</p> Lectura desde archivo JSON Crear una tabla desde archivo<pre><code>${table}=    PyTabify.Create Data Table From File    people.json\n${headers}=    PyTabify.Get Data Table Headers    ${table}\n</code></pre>"},{"location":"getting-started/installation/","title":"Instalacion","text":"<p>Instala <code>pytabify</code> con el camino que corresponda a tu uso. Esta pagina cubre prerequisitos, pasos minimos, verificacion y errores frecuentes.</p>"},{"location":"getting-started/installation/#prerequisitos","title":"Prerequisitos","text":"<ul> <li> <p>Python</p> <p><code>pytabify</code> requiere <code>Python &gt;= 3.10</code>.</p> </li> <li> <p>pip o Poetry</p> <p>Usa <code>pip</code> si solo vas a consumir la libreria. Usa <code>poetry</code> si vas a desarrollar o iterar en la documentacion del proyecto.</p> </li> <li> <p>openpyxl</p> <p>Se instala como dependencia del paquete y habilita lectura y escritura de <code>XLSX</code>.</p> </li> </ul>"},{"location":"getting-started/installation/#instalacion-paso-a-paso","title":"Instalacion paso a paso","text":"UsuarioDesarrollo 1. Instalar con pip<pre><code>pip install pytabify\n</code></pre> 2. Verificar la instalacion<pre><code>python -c \"from pytabify import DataTableCreator, DataTableSaver; print('ok')\"\n</code></pre> 1. Instalar dependencias con Poetry<pre><code>poetry install\n</code></pre> 2. Verificar el entorno<pre><code>poetry run python -c \"from pytabify import DataTableCreator, DataTableSaver; print('ok')\"\n</code></pre> 3. Levantar la documentacion local<pre><code>poetry run mkdocs serve\n</code></pre>"},{"location":"getting-started/installation/#configuracion-inicial","title":"Configuracion inicial","text":"<p><code>pytabify</code> no requiere archivo de configuracion, variables de entorno ni bootstrap adicional para empezar a usar la API publica.</p> <p>Que si necesitas preparar</p> <p>Lo unico que debes tener listo es el origen de datos con el que vas a trabajar: un archivo <code>csv</code>, <code>json</code> o <code>xlsx</code>, o una lista de diccionarios en memoria.</p>"},{"location":"getting-started/installation/#comandos-basicos","title":"Comandos basicos","text":"Comando Cuando usarlo <code>pip install pytabify</code> instalar la libreria para consumo <code>poetry install</code> preparar el entorno de desarrollo <code>python -c \"from pytabify import DataTableCreator; print('ok')\"</code> verificar instalacion minima <code>poetry run mkdocs serve</code> iterar localmente sobre la documentacion <code>poetry run mkdocs build</code> validar que el sitio compila"},{"location":"getting-started/installation/#verificacion-minima","title":"Verificacion minima","text":"Paquete instaladoSitio de documentacion Comprobar import basico<pre><code>python -c \"from pytabify import DataTableCreator, DataTableSaver; print('ok')\"\n</code></pre> Levantar documentacion local<pre><code>poetry run mkdocs serve\n</code></pre> Generar el sitio estatico<pre><code>poetry run mkdocs build\n</code></pre> <p>Cuando usar cada comando</p> <p>Usa <code>mkdocs serve</code> para iterar localmente y <code>mkdocs build</code> para validar que el sitio compila sin errores.</p>"},{"location":"getting-started/installation/#ejemplos-de-ejecucion","title":"Ejemplos de ejecucion","text":"Consumo simpleEntorno del proyecto Instalar y validar<pre><code>pip install pytabify\npython -c \"from pytabify import DataTableCreator; print('ok')\"\n</code></pre> Preparar repo y levantar docs<pre><code>poetry install\npoetry run mkdocs serve\n</code></pre>"},{"location":"getting-started/installation/#parametros-importantes","title":"Parametros importantes","text":"<p>Aunque la instalacion no expone muchos parametros propios, estos son los mas relevantes cuando empieces a ejecutar la libreria:</p> Parametro Donde aparece Para que sirve <code>path</code> <code>DataTableCreator.from_file(...)</code> indica el archivo de entrada <code>sheet_name</code> lectura de <code>XLSX</code> selecciona la hoja a leer <code>encoding</code> lectura y escritura de <code>CSV</code> o <code>JSON</code> controla codificacion del archivo Cuando mirar estos parametros <p>Si la libreria ya instala bien pero el primer flujo falla al cargar o guardar archivos, casi siempre el problema real esta en <code>path</code>, <code>sheet_name</code> o <code>encoding</code>.</p> Dependencias de desarrollo relevantes Dependencia Para que sirve <code>pytest</code> pruebas unitarias y de aceptacion <code>ruff</code> lint y formato <code>mypy</code> chequeo estatico <code>mkdocs-material</code> sitio tecnico de documentacion Entorno sin dependencias de desarrollo <p>Si <code>poetry run mkdocs build</code> falla porque <code>mkdocs</code> no existe, el entorno se creo sin el grupo de desarrollo. Reinstala con <code>poetry install</code>.</p>"},{"location":"getting-started/installation/#errores-frecuentes-de-instalacion-o-ejecucion","title":"Errores frecuentes de instalacion o ejecucion","text":"<p>Python incompatible</p> <p>Si el entorno usa una version menor a <code>3.10</code>, la instalacion o la ejecucion puede fallar. Verifica la version activa con <code>python --version</code>.</p> <p><code>mkdocs</code> no encontrado</p> <p>Si <code>poetry run mkdocs build</code> falla, el entorno no tiene dependencias de desarrollo instaladas. Ejecuta <code>poetry install</code>.</p> <p>Lectura de XLSX sin <code>sheet_name</code></p> <p>La libreria se instala bien, pero el flujo de lectura falla si intentas abrir un <code>xlsx</code> sin indicar la hoja.</p> <p>Extension no soportada</p> <p><code>pytabify</code> solo resuelve <code>csv</code>, <code>json</code> y <code>xlsx</code>. Si pasas otra extension, el problema no es de instalacion sino del formato de entrada.</p>"},{"location":"getting-started/installation/#siguiente-paso","title":"Siguiente paso","text":"<p>Ir al inicio rapido Ver ejemplos reales</p>"},{"location":"getting-started/quickstart/","title":"Inicio rapido","text":"<p>Este recorrido cubre el flujo minimo completo: cargar datos, inspeccionar filas, mutar el esquema y guardar en otro formato.</p>"},{"location":"getting-started/quickstart/#flujo-minimo-de-principio-a-fin","title":"Flujo minimo de principio a fin","text":"<p>Este ejemplo toma un archivo <code>JSON</code>, lo carga en memoria, agrega una columna nueva y lo exporta a <code>CSV</code>. El mismo flujo aparece en Python y en Robot Framework.</p>"},{"location":"getting-started/quickstart/#input","title":"Input","text":"people.json<pre><code>[\n  {\"name\": \"Alice\", \"age\": 30},\n  {\"name\": \"Bob\", \"age\": 25}\n]\n</code></pre>"},{"location":"getting-started/quickstart/#comando","title":"Comando","text":"PythonRobot Framework quickstart.py<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_file(\"people.json\")\n\nprint(datatable.column_names)\nprint(datatable[0].to_dict())\n\ndatatable[0][\"country\"] = \"MX\"\n\nDataTableSaver.into_csv(datatable, \"people.csv\")\n</code></pre> Ejecucion<pre><code>python quickstart.py\n</code></pre> quickstart.robot<pre><code>*** Settings ***\nLibrary    pytabify.robot.PyTabifyLibrary    WITH NAME    PyTabify\n\n*** Test Cases ***\nConvertir json a csv\n    ${table}=    PyTabify.Create Data Table From File    people.json\n    ${headers}=    PyTabify.Get Data Table Headers    ${table}\n    Log To Console    ${headers}\n    ${row}=    PyTabify.Get Data Table Row    ${table}    0\n    Log To Console    ${row.to_dict()}\n    ${table}=    PyTabify.Set Data Table Value    ${table}    0    country    MX\n    PyTabify.Save Data Table To Csv    ${table}    people.csv\n</code></pre> Ejecucion<pre><code>robot quickstart.robot\n</code></pre>"},{"location":"getting-started/quickstart/#output-esperado","title":"Output esperado","text":"PythonRobot Framework Salida en consolaArchivo generado stdout<pre><code>('name', 'age')\n{'name': 'Alice', 'age': 30}\n</code></pre> people.csv<pre><code>name,age,country\nAlice,30,MX\nBob,25,\n</code></pre> Salida en consolaArchivo generado stdout<pre><code>['name', 'age']\n{'name': 'Alice', 'age': 30}\n</code></pre> people.csv<pre><code>name,age,country\nAlice,30,MX\nBob,25,\n</code></pre> <p>Que demuestra este flujo</p> <p>El ejemplo muestra las tres operaciones centrales de <code>pytabify</code>: lectura, mutacion con esquema estable y persistencia en otro formato.</p> Variantes del mismo flujo Desde archivoDesde registros JSON -&gt; DataTable -&gt; CSV<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_file(\"people.json\")\n\nprint(datatable.column_names)\nprint(datatable[0].to_dict())\n\ndatatable[0][\"country\"] = \"MX\"\n\nDataTableSaver.into_csv(datatable, \"people.csv\")\n</code></pre> Lista de diccionarios -&gt; DataTable -&gt; JSON<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_records(\n    [\n        {\"name\": \"Alice\", \"age\": 30},\n        {\"name\": \"Bob\", \"age\": 25},\n    ]\n)\n\ndatatable[1].country = \"US\"\n\nDataTableSaver.into_json(datatable, \"people.json\")\n</code></pre>"},{"location":"getting-started/quickstart/#que-pasa-en-memoria","title":"Que pasa en memoria","text":"Acceso por indice, atributo y llave<pre><code>row = datatable[0]\n\nprint(row.name.value)\nprint(row[\"age\"].value)\nprint(row.to_dict())\n</code></pre> <p>Mutacion segura del esquema</p> <p>Si agregas una columna nueva en una fila, <code>pytabify</code> extiende el esquema completo y rellena <code>None</code> en las demas filas.</p>"},{"location":"getting-started/quickstart/#parametros-importantes-de-uso-basico","title":"Parametros importantes de uso basico","text":"Parametro Aplica a Para que sirve <code>path</code> <code>from_file</code>, <code>into_csv</code>, <code>into_json</code>, <code>into_xlsx</code> define archivo de entrada o salida <code>sheet_name</code> <code>from_file</code> con <code>XLSX</code> indica la hoja especifica a leer <code>encoding</code> lectura y escritura de <code>CSV</code> o <code>JSON</code> controla la codificacion del archivo <code>records</code> <code>from_records</code> recibe la coleccion tabular inicial Parametro <code>path</code>Parametro <code>sheet_name</code>Parametro <code>encoding</code> Cargar desde archivo<pre><code>datatable = DataTableCreator.from_file(\"people.json\")\n</code></pre> Leer una hoja concreta<pre><code>datatable = DataTableCreator.from_file(\"people.xlsx\", sheet_name=\"People\")\n</code></pre> Guardar JSON con codificacion explicita<pre><code>DataTableSaver.into_json(datatable, \"people.json\", encoding=\"utf-8\")\n</code></pre>"},{"location":"getting-started/quickstart/#resultado-esperado","title":"Resultado esperado","text":"InputOutput people.json<pre><code>[\n  {\"name\": \"Alice\", \"age\": 30},\n  {\"name\": \"Bob\", \"age\": 25}\n]\n</code></pre> people-enriched.json<pre><code>[\n  {\"name\": \"Alice\", \"age\": 30, \"country\": \"MX\"},\n  {\"name\": \"Bob\", \"age\": 25, \"country\": null}\n]\n</code></pre>"},{"location":"getting-started/quickstart/#reglas-practicas","title":"Reglas practicas","text":"<ul> <li> <p><code>CSV</code></p> <p>Bueno para interoperabilidad rapida. Al leer, los valores llegan como texto.</p> </li> <li> <p><code>JSON</code></p> <p>Buena opcion cuando quieres conservar tipos nativos simples.</p> </li> <li> <p><code>XLSX</code></p> <p>Necesita <code>sheet_name</code> al leer desde archivo.</p> </li> </ul> Lectura de XLSX Lectura de una hoja especifica<pre><code>datatable = DataTableCreator.from_file(\"people.xlsx\", sheet_name=\"People\")\n</code></pre>"},{"location":"getting-started/quickstart/#errores-frecuentes-en-uso-basico","title":"Errores frecuentes en uso basico","text":"<p>Archivo inexistente</p> <p>Si <code>path</code> no apunta a un archivo real, la carga falla aunque el formato sea correcto.</p> <p>Esquema no rectangular en <code>from_records</code></p> <p>Todas las filas deben compartir el mismo conjunto de columnas. Si una fila cambia el esquema, la validacion falla.</p> <p>Tipos distintos al leer <code>CSV</code></p> <p>Al leer <code>CSV</code>, los valores entran como texto. Si necesitas preservar tipos nativos, usa <code>JSON</code>, <code>XLSX</code> o <code>from_records</code>.</p> Cuando subir de nivel <p>Si el flujo basico ya te queda corto, el siguiente salto natural es revisar DataTableCreator y DataTableSaver para entender mejor cada variante.</p>"},{"location":"getting-started/quickstart/#despues-del-quickstart","title":"Despues del quickstart","text":"<p>Ejemplos en Python Ejemplos en Robot Framework Referencia de DataTableCreator</p>"},{"location":"internal/architecture/","title":"Arquitectura","text":"<p>Solo si vas a extender la libreria</p> <p>Para usar <code>pytabify</code> no necesitas esta pagina. Esta seccion existe para entender como se compone internamente la API publica y donde encajan nuevos adaptadores.</p>"},{"location":"internal/architecture/#vista-general","title":"Vista general","text":"<pre><code>flowchart LR\n    User[\"Usuario / tests\"] --&gt; Facade[\"Fachadas publicas&lt;br/&gt;DataTableCreator / DataTableSaver\"]\n    Facade --&gt; Bootstrap[\"bootstrap.py\"]\n    Bootstrap --&gt; UseCases[\"Casos de uso\"]\n    UseCases --&gt; Ports[\"Puertos\"]\n    Ports --&gt; Adapters[\"Adaptadores de archivos\"]\n    UseCases --&gt; Domain[\"Dominio tabular\"]\n    Robot[\"PyTabifyLibrary\"] --&gt; Bootstrap\n    Robot --&gt; Domain</code></pre>"},{"location":"internal/architecture/#componentes-principales","title":"Componentes principales","text":"Componente Responsabilidad <code>creator.py</code> entrada publica para crear <code>DataTable</code> desde archivo o registros <code>saver.py</code> entrada publica para persistir <code>DataTable</code> <code>bootstrap.py</code> compone casos de uso con resolvers concretos <code>application/use_cases</code> orquesta los flujos de carga y guardado <code>application/ports</code> define contratos para readers, writers y resolvers <code>domain</code> define <code>DataTable</code>, filas, campos y validacion tabular <code>adapters/files/readers</code> implementa lectura para <code>CSV</code>, <code>JSON</code> y <code>XLSX</code> <code>adapters/files/writers</code> implementa escritura para <code>CSV</code>, <code>JSON</code> y <code>XLSX</code> <code>robot/</code> wrapper oficial y adaptadores para Robot Framework"},{"location":"internal/architecture/#flujo-de-datos-principal","title":"Flujo de datos principal","text":""},{"location":"internal/architecture/#carga-desde-archivo","title":"Carga desde archivo","text":"<pre><code>sequenceDiagram\n    participant U as Usuario\n    participant C as DataTableCreator\n    participant B as bootstrap.py\n    participant UC as CreateDataTableFromFile\n    participant R as FileReaderResolver\n    participant A as ReaderAdapter\n    participant D as Domain\n\n    U-&gt;&gt;C: from_file(path, **kwargs)\n    C-&gt;&gt;B: build_create_table_from_file_use_case()\n    B-&gt;&gt;UC: instancia caso de uso\n    UC-&gt;&gt;R: resolver reader por extension\n    R-&gt;&gt;A: reader concreto\n    A--&gt;&gt;UC: list[dict[str, Any]]\n    UC-&gt;&gt;D: validar y construir DataTable\n    D--&gt;&gt;U: DataTable</code></pre>"},{"location":"internal/architecture/#persistencia","title":"Persistencia","text":"<pre><code>sequenceDiagram\n    participant U as Usuario\n    participant S as DataTableSaver\n    participant B as bootstrap.py\n    participant UC as SaveDataTable\n    participant R as FileWriterResolver\n    participant A as WriterAdapter\n\n    U-&gt;&gt;S: into_json / into_csv / into_xlsx\n    S-&gt;&gt;B: build_save_table_use_case()\n    B-&gt;&gt;UC: instancia caso de uso\n    UC-&gt;&gt;R: resolver writer por extension\n    R-&gt;&gt;A: writer concreto\n    A--&gt;&gt;U: archivo persistido</code></pre>"},{"location":"internal/architecture/#lo-importante-para-no-romper-el-diseno","title":"Lo importante para no romper el diseno","text":"<ul> <li> <p>El dominio no depende de archivos</p> <p><code>DataTable</code> y sus reglas siguen siendo independientes de CSV, JSON o Excel.</p> </li> <li> <p>Los formatos viven en adaptadores</p> <p>Cambiar o agregar un formato afecta resolvers y adaptadores, no la API publica completa.</p> </li> <li> <p>Las fachadas deben seguir pequenas</p> <p><code>DataTableCreator</code> y <code>DataTableSaver</code> exponen intencion de uso, no detalles de infraestructura.</p> </li> </ul>"},{"location":"internal/architecture/#decisiones-de-diseno-relevantes","title":"Decisiones de dise\u00f1o relevantes","text":"<p>Fachadas publicas pequenas</p> <p>La API publica se mantiene corta a proposito. <code>DataTableCreator</code> y <code>DataTableSaver</code> exponen intencion de uso y evitan que el consumidor dependa de adaptadores o resolvers concretos.</p> <p>Validacion centralizada en el dominio</p> <p>Los adaptadores convierten datos externos a estructuras simples. La validacion del contrato tabular vive en el dominio, no repartida entre readers y writers.</p> <p>Resolucion por extension</p> <p>La infraestructura decide readers y writers a partir de la extension del archivo. Eso simplifica el uso, pero obliga a que <code>path</code> sea coherente con el formato real.</p>"},{"location":"internal/architecture/#donde-entra-robot-framework","title":"Donde entra Robot Framework","text":"<p><code>PyTabifyLibrary</code> reutiliza los mismos casos de uso y envuelve la tabla nativa en <code>RobotDataTable</code> y <code>RobotDataRow</code>. Eso evita duplicar logica de negocio y mantiene un contrato coherente entre Python y Robot.</p>"},{"location":"internal/architecture/#como-extender-o-mantener-la-herramienta","title":"Como extender o mantener la herramienta","text":""},{"location":"internal/architecture/#extender-formatos","title":"Extender formatos","text":"<ol> <li>Agrega un reader o writer concreto en <code>adapters/files/readers</code> o <code>adapters/files/writers</code>.</li> <li>Registra el nuevo formato en el resolver correspondiente.</li> <li>Mant\u00e9n la salida del reader como <code>list[dict[str, Any]]</code>.</li> <li>Verifica que la API publica siga entrando por <code>DataTableCreator</code> y <code>DataTableSaver</code>.</li> </ol>"},{"location":"internal/architecture/#mantener-el-diseno","title":"Mantener el diseno","text":"<ol> <li>Evita mover logica de validacion al adaptador.</li> <li>No hagas crecer las fachadas publicas con casos demasiado especificos.</li> <li>Mant\u00e9n los tests end-to-end alineados con la API publica, no con detalles internos.</li> <li>Si agregas un nuevo flujo, decide primero si pertenece a <code>domain</code>, <code>application</code> o <code>adapters</code>.</li> </ol> Checklist corto para mantenedores <ul> <li>Si cambias el contrato tabular, revisa dominio, tests unitarios y ejemplos publicos.</li> <li>Si cambias un formato, revisa resolver, adaptador y al menos un round-trip.</li> <li>Si cambias la API publica, revisa Python, Robot Framework y documentacion.</li> </ul> Composicion interna real de casos de uso<pre><code>from pytabify.adapters.files.resolvers import FileReaderResolver, FileWriterResolver\nfrom pytabify.application.use_cases.create_data_table_from_file import CreateDataTableFromFile\nfrom pytabify.application.use_cases.create_data_table_from_records import CreateDataTableFromRecords\nfrom pytabify.application.use_cases.save_data_table import SaveDataTable\n\n\ndef build_create_table_from_file_use_case() -&gt; CreateDataTableFromFile:\n    return CreateDataTableFromFile(reader_resolver=FileReaderResolver())\n\n\ndef build_create_table_from_records_use_case() -&gt; CreateDataTableFromRecords:\n    return CreateDataTableFromRecords()\n\n\ndef build_save_table_use_case() -&gt; SaveDataTable:\n    return SaveDataTable(writer_resolver=FileWriterResolver())\n</code></pre> <p>Cambio de arquitectura que suele degradar el proyecto</p> <p>Si un adaptador empieza a decidir reglas de negocio o el dominio empieza a depender de formatos concretos, la separacion actual se rompe rapido. Ese es el principal limite a proteger en mantenimiento.</p> <p>Extender formatos</p>"},{"location":"internal/extending-readers-writers/","title":"Extender formatos","text":"<p>Si agregas un nuevo formato, el cambio debe respetar la separacion actual entre dominio, casos de uso y adaptadores.</p>"},{"location":"internal/extending-readers-writers/#donde-tocar","title":"Donde tocar","text":"Area Rol <code>src/pytabify/application/ports</code> contratos de lectura y escritura <code>src/pytabify/application/use_cases</code> flujos de carga y persistencia <code>src/pytabify/adapters/files/readers</code> readers concretos por formato <code>src/pytabify/adapters/files/writers</code> writers concretos por formato <code>src/pytabify/adapters/files/resolvers.py</code> resolucion por extension"},{"location":"internal/extending-readers-writers/#flujo-recomendado","title":"Flujo recomendado","text":"<ol> <li>Implementa un reader o writer concreto en <code>adapters/files</code>.</li> <li>Registra el formato en el resolver correspondiente.</li> <li>Mant\u00e9n intacto el contrato del dominio.</li> <li>Agrega pruebas unitarias del adaptador y al menos un flujo end-to-end.</li> </ol> Reader existenteNuevo reader <ul> <li>resuelve archivo por extension;</li> <li>convierte origen externo a <code>list[dict[str, Any]]</code>;</li> <li>deja al dominio validar el contrato tabular.</li> </ul> <ul> <li>debe seguir el mismo contrato de salida;</li> <li>no debe inyectar reglas de negocio en la infraestructura;</li> <li>debe fallar con errores de infraestructura coherentes.</li> </ul> <p>No rompas el contrato del dominio</p> <p>Si tu adaptador produce filas no rectangulares o nombres de columnas inconsistentes, el problema no se arregla en la capa de dominio. El adaptador debe entregar una estructura razonable para que la validacion haga su trabajo.</p> Pruebas minimas recomendadas <ul> <li>lectura o escritura exitosa del nuevo formato;</li> <li>error consistente cuando el archivo no existe;</li> <li>error consistente cuando el contenido es invalido;</li> <li>compatibilidad con <code>DataTableCreator</code> o <code>DataTableSaver</code> a traves de los resolvers.</li> </ul>"},{"location":"reference/creator/","title":"DataTableCreator","text":"<p>Fachada publica para construir un <code>DataTable</code> desde archivo o desde registros en memoria.</p>"},{"location":"reference/creator/#operaciones-disponibles","title":"Operaciones disponibles","text":"Metodo Entrada Uso recomendado <code>from_file(path, **kwargs)</code> archivo <code>CSV</code>, <code>JSON</code> o <code>XLSX</code> ingestar datos ya persistidos <code>from_records(records)</code> lista de diccionarios construir datos tabulares en memoria <p>Referencia rapida</p> <p>Si ya instalaste la libreria, esta es la entrada correcta para crear tablas en escenarios reales. No necesitas pasar por infraestructura interna.</p>"},{"location":"reference/creator/#from_filepath-kwargs","title":"<code>from_file(path, **kwargs)</code>","text":"JSONCSVXLSX Cargar JSON<pre><code>from pytabify import DataTableCreator\n\ndatatable = DataTableCreator.from_file(\"people.json\")\n</code></pre> Cargar CSV<pre><code>from pytabify import DataTableCreator\n\ndatatable = DataTableCreator.from_file(\"people.csv\", encoding=\"utf-8\")\n</code></pre> Cargar XLSX<pre><code>from pytabify import DataTableCreator\n\ndatatable = DataTableCreator.from_file(\"people.xlsx\", sheet_name=\"People\")\n</code></pre>"},{"location":"reference/creator/#parametros-practicos","title":"Parametros practicos","text":"Parametro Requerido Aplica a Nota <code>path</code> Si todos determina el reader por extension <code>sheet_name</code> Si para lectura <code>XLSX</code> <code>XLSX</code> sin este valor la lectura falla <code>encoding</code> No <code>CSV</code>, <code>JSON</code> por defecto usa <code>utf-8</code>"},{"location":"reference/creator/#explicacion-breve-de-cada-argumento","title":"Explicacion breve de cada argumento","text":"<ul> <li><code>path</code>: ruta del archivo de entrada.</li> <li><code>sheet_name</code>: nombre exacto de la hoja cuando el archivo es <code>xlsx</code>.</li> <li><code>encoding</code>: codificacion para lectura de <code>csv</code> o <code>json</code>.</li> </ul>"},{"location":"reference/creator/#combinaciones-validas","title":"Combinaciones validas","text":"ValidoInvalido Lecturas correctas<pre><code>DataTableCreator.from_file(\"people.json\")\nDataTableCreator.from_file(\"people.csv\", encoding=\"utf-8\")\nDataTableCreator.from_file(\"people.xlsx\", sheet_name=\"People\")\n</code></pre> Combinaciones que fallan<pre><code>DataTableCreator.from_file(\"people.xlsx\")\nDataTableCreator.from_file(\"people.txt\")\n</code></pre> Lectura de XLSX Cargar una hoja especifica<pre><code>datatable = DataTableCreator.from_file(\"people.xlsx\", sheet_name=\"People\")\n</code></pre> <p>Extension no soportada</p> <p>Si la extension no es <code>csv</code>, <code>json</code> o <code>xlsx</code>, el flujo termina con una excepcion de infraestructura.</p> Errores comunes de sintaxis o combinacion <ul> <li>Olvidar <code>sheet_name</code> al leer <code>XLSX</code>.</li> <li>Pasar una extension no soportada en <code>path</code>.</li> <li>Suponer que <code>encoding</code> aplica a <code>XLSX</code>.</li> <li>Esperar tipos nativos al leer <code>CSV</code>.</li> </ul>"},{"location":"reference/creator/#from_recordsrecords","title":"<code>from_records(records)</code>","text":"Crear una tabla desde memoria<pre><code>datatable = DataTableCreator.from_records(\n    [\n        {\"name\": \"Alice\", \"age\": 30},\n        {\"name\": \"Bob\", \"age\": 25},\n    ]\n)\n</code></pre>"},{"location":"reference/creator/#contrato-esperado","title":"Contrato esperado","text":"<ul> <li> <p>Entrada valida</p> <p>Una lista de diccionarios con el mismo esquema tabular.</p> </li> <li> <p>Orden estable</p> <p>El orden de columnas queda anclado al primer registro valido.</p> </li> <li> <p>Normalizacion</p> <p>Los nombres de columnas se normalizan a <code>str</code>.</p> </li> </ul> ValidoInvalido Esquema consistente<pre><code>[\n    {\"name\": \"Alice\", \"age\": 30},\n    {\"age\": 25, \"name\": \"Bob\"},\n]\n</code></pre> Esquema no rectangular<pre><code>[\n    {\"name\": \"Alice\", \"age\": 30},\n    {\"name\": \"Bob\", \"country\": \"MX\"},\n]\n</code></pre>"},{"location":"reference/creator/#ejemplos-reales-de-uso","title":"Ejemplos reales de uso","text":"Preparar fixturesCargar un origen y seguir procesando Tabla desde memoria para pruebas<pre><code>datatable = DataTableCreator.from_records(\n    [\n        {\"name\": \"Alice\", \"active\": True},\n        {\"name\": \"Bob\", \"active\": False},\n    ]\n)\n</code></pre> Usar la tabla en memoria<pre><code>datatable = DataTableCreator.from_file(\"people.json\")\nfirst_row = datatable[0].to_dict()\nheaders = datatable.column_names\n</code></pre> <p>Cuando elegir <code>from_records</code></p> <p>Es la mejor opcion cuando el origen real de datos ya esta en memoria o cuando quieres preparar fixtures para pruebas sin depender de archivos temporales.</p>"},{"location":"reference/creator/#buenas-practicas-de-uso","title":"Buenas practicas de uso","text":"<ul> <li>Usa <code>from_records</code> cuando el origen ya este en memoria.</li> <li>Usa <code>from_file</code> cuando la fuente real sea un archivo del que quieres conservar estructura.</li> <li>Mant\u00e9n un esquema consistente en <code>records</code> y agrega columnas nuevas despues sobre la tabla, no antes con filas irregulares.</li> <li>Para datos tipados, prefiere <code>JSON</code>, <code>XLSX</code> o <code>from_records</code> frente a <code>CSV</code>.</li> </ul> <p>Guardar tablas Ver DataTable</p>"},{"location":"reference/data-table/","title":"DataTable","text":"<p><code>DataTable</code> es el contrato tabular en memoria. Mantiene un esquema canonico estable y filas sincronizadas con ese esquema.</p>"},{"location":"reference/data-table/#lectura","title":"Lectura","text":"Acceso por indiceAcceso por atributoAcceso por llave Obtener una fila<pre><code>row = datatable[0]\nprint(row.to_dict())\n</code></pre> Leer un campo<pre><code>print(datatable[0].name.value)\n</code></pre> Leer un campo por columna<pre><code>print(datatable[0][\"age\"].value)\n</code></pre>"},{"location":"reference/data-table/#escritura","title":"Escritura","text":"Actualizar columna existenteAgregar columna nueva Mutar una celda sin duplicar columnas<pre><code>datatable[0][\"age\"] = 31\n</code></pre> Expandir el esquema<pre><code>datatable[0][\"country\"] = \"MX\"\n</code></pre> Efecto en toda la tabla<pre><code>print(datatable[0].country.value)  # MX\nprint(datatable[1].country.value)  # None\n</code></pre>"},{"location":"reference/data-table/#operaciones-utiles","title":"Operaciones utiles","text":"Operacion Resultado <code>len(datatable)</code> total de filas <code>datatable.row(index)</code> fila por indice <code>datatable.column_names</code> tupla con nombres de columna <code>datatable.headers()</code> headers con nombre e indice <code>datatable.to_dict()</code> lista serializable de registros"},{"location":"reference/data-table/#ejemplos-reales-de-uso","title":"Ejemplos reales de uso","text":"Inspeccionar encabezadosMutar y serializar Ver schema actual<pre><code>print(datatable.column_names)\nprint([header.name for header in datatable.headers()])\n</code></pre> Agregar una columna y exportar a dict<pre><code>datatable[0][\"country\"] = \"MX\"\nprint(datatable.to_dict())\n</code></pre> <p>Schema-first</p> <p>El esquema no depende de la fila que leas despues. Queda definido por la validacion inicial y por las expansiones controladas que hagas sobre la tabla.</p> Lo que no debes asumir <ul> <li>No asumas que leer desde <code>CSV</code> preserva tipos nativos.</li> <li>No asumas que una columna ausente en una fila puede omitirse sin afectar el contrato.</li> <li>No asumas que una columna inexistente devolvera <code>None</code>; en lectura directa puede lanzar <code>KeyError</code> o <code>AttributeError</code>.</li> </ul>"},{"location":"reference/data-table/#buenas-practicas-de-uso","title":"Buenas practicas de uso","text":"<ul> <li>Lee columnas existentes por atributo o llave segun el caso, pero no mezcles supuestos sobre columnas faltantes.</li> <li>Cuando necesites una salida serializable, usa <code>to_dict()</code> en lugar de reconstruir registros manualmente.</li> <li>Agrega columnas nuevas sobre la tabla ya creada para que el schema se propague correctamente.</li> </ul> <p>Crear tablas Guardar tablas</p>"},{"location":"reference/file-formats/","title":"Formatos soportados","text":"<p><code>pytabify</code> resuelve un problema muy concreto: mover datos tabulares entre memoria y archivos con un contrato estable.</p>"},{"location":"reference/file-formats/#vista-rapida","title":"Vista rapida","text":"Formato Leer Guardar Conserva tipos Consideracion principal <code>CSV</code> Si Si Limitado todo entra como texto al leer <code>JSON</code> Si Si Si, para tipos simples buen round-trip para APIs y fixtures <code>XLSX</code> Si Si Si, segun el contenido de celdas requiere <code>sheet_name</code> al leer"},{"location":"reference/file-formats/#cuando-usar-cada-formato","title":"Cu\u00e1ndo usar cada formato","text":"<ul> <li> <p>CSV</p> <p>Mejor cuando necesitas interoperar rapido con otras herramientas o entregar un archivo plano.</p> </li> <li> <p>JSON</p> <p>Mejor cuando quieres preservar estructura y tipos simples con friccion baja.</p> </li> <li> <p>XLSX</p> <p>Mejor cuando el origen o destino natural del usuario es Excel.</p> </li> </ul>"},{"location":"reference/file-formats/#reglas-practicas-por-formato","title":"Reglas practicas por formato","text":"CSVJSONXLSX <ul> <li>El reader devuelve valores como texto.</li> <li>Usa <code>encoding=\"utf-8\"</code> salvo necesidad puntual.</li> <li>Es el formato menos expresivo para <code>None</code> y booleanos.</li> </ul> <ul> <li>Funciona bien para pruebas, fixtures y round-trip simple.</li> <li>Mantiene mejor enteros, booleanos y <code>null</code>.</li> </ul> <ul> <li>Necesita <code>sheet_name</code> para leer.</li> <li>La primera fila se interpreta como encabezado.</li> <li>Es buena opcion cuando tu fuente ya vive en Excel.</li> </ul> <p>Extension valida</p> <p>Los resolvers de infraestructura trabajan por extension de archivo. Si cambias la extension manualmente a un formato no soportado, la lectura o escritura fallara.</p>"},{"location":"reference/saver/","title":"DataTableSaver","text":"<p>Fachada publica para persistir un <code>DataTable</code> en distintos formatos.</p>"},{"location":"reference/saver/#operaciones-disponibles","title":"Operaciones disponibles","text":"Metodo Salida Nota <code>into_csv(datatable, path, encoding=\"utf-8\")</code> <code>CSV</code> ideal para interoperabilidad <code>into_json(datatable, path, encoding=\"utf-8\")</code> <code>JSON</code> conserva mejor tipos simples <code>into_xlsx(datatable, path, encoding=\"utf-8\")</code> <code>XLSX</code> salida apta para hojas de calculo"},{"location":"reference/saver/#explicacion-breve-de-cada-opcion","title":"Explicacion breve de cada opcion","text":"<ul> <li><code>datatable</code>: tabla nativa que quieres persistir.</li> <li><code>path</code>: ruta y nombre del archivo de salida.</li> <li><code>encoding</code>: codificacion para <code>CSV</code> y <code>JSON</code>.</li> </ul>"},{"location":"reference/saver/#guardar-el-mismo-datatable-en-varios-formatos","title":"Guardar el mismo DataTable en varios formatos","text":"Persistir una misma tabla<pre><code>from pytabify import DataTableCreator, DataTableSaver\n\ndatatable = DataTableCreator.from_records(\n    [\n        {\"name\": \"Alice\", \"age\": 30},\n        {\"name\": \"Bob\", \"age\": 25},\n    ]\n)\n\nDataTableSaver.into_csv(datatable, \"people.csv\")\nDataTableSaver.into_json(datatable, \"people.json\")\nDataTableSaver.into_xlsx(datatable, \"people.xlsx\")\n</code></pre> CSVJSONXLSX people.csv<pre><code>name,age\nAlice,30\nBob,25\n</code></pre> people.json<pre><code>[\n  {\"name\": \"Alice\", \"age\": 30},\n  {\"name\": \"Bob\", \"age\": 25}\n]\n</code></pre> <p>El archivo se genera con encabezados en la primera fila y luego los registros en orden de esquema.</p>"},{"location":"reference/saver/#parametros","title":"Parametros","text":"Parametro Requerido Descripcion <code>datatable</code> Si instancia nativa de <code>DataTable</code> <code>path</code> Si ruta de salida <code>encoding</code> No relevante para <code>CSV</code> y <code>JSON</code>"},{"location":"reference/saver/#variantes-reales-de-uso","title":"Variantes reales de uso","text":"Exportar a CSVExportar a JSONExportar a XLSX Salida interoperable<pre><code>DataTableSaver.into_csv(datatable, \"people.csv\")\n</code></pre> Salida con tipos simples<pre><code>DataTableSaver.into_json(datatable, \"people.json\", encoding=\"utf-8\")\n</code></pre> Salida para Excel<pre><code>DataTableSaver.into_xlsx(datatable, \"people.xlsx\")\n</code></pre> Ejemplo secundario Convertir JSON a XLSX<pre><code>datatable = DataTableCreator.from_file(\"people.json\")\nDataTableSaver.into_xlsx(datatable, \"people.xlsx\")\n</code></pre> <p>Tipos nativos</p> <p><code>JSON</code> y <code>XLSX</code> representan mejor <code>int</code>, <code>bool</code> y <code>None</code> que <code>CSV</code>.</p> Errores de escritura <p>Si la ruta no se puede escribir o el adaptador de salida falla, la operacion termina con una excepcion de escritura. Revisa permisos, ruta y extension objetivo.</p>"},{"location":"reference/saver/#errores-comunes-de-sintaxis-o-combinacion","title":"Errores comunes de sintaxis o combinacion","text":"<ul> <li>Usar una extension no soportada en <code>path</code>.</li> <li>Esperar que <code>encoding</code> modifique la escritura de <code>XLSX</code>.</li> <li>Intentar guardar una estructura que no sea un <code>DataTable</code> nativo.</li> <li>Asumir que <code>CSV</code> conserva tipos igual que <code>JSON</code> o <code>XLSX</code>.</li> </ul>"},{"location":"reference/saver/#buenas-practicas-de-uso","title":"Buenas practicas de uso","text":"<ul> <li>Usa una extension coherente con el formato que quieres producir.</li> <li>Si el archivo va a consumirse fuera de Python, <code>CSV</code> suele ser la salida mas portable.</li> <li>Si necesitas round-trip con tipos simples, prioriza <code>JSON</code>.</li> <li>Si el destino natural es Excel, usa <code>into_xlsx</code> en lugar de generar <code>CSV</code> por costumbre.</li> </ul> <p>Ver DataTable Formatos soportados</p>"},{"location":"troubleshooting/common-errors/","title":"Errores comunes","text":"<p>Esta pagina concentra las fallas mas probables al usar <code>pytabify</code> y la forma rapida de resolverlas.</p>"},{"location":"troubleshooting/common-errors/#extension-no-soportada","title":"Extension no soportada","text":"<p>Sintoma</p> <p>Intentas leer o guardar un archivo con una extension distinta de <code>csv</code>, <code>json</code> o <code>xlsx</code>.</p> <p>Causa Los resolvers seleccionan adaptadores por extension.</p> <p>Solucion Usa una extension soportada y consistente con el formato real del archivo.</p>"},{"location":"troubleshooting/common-errors/#sheet_name-faltante-en-xlsx","title":"<code>sheet_name</code> faltante en XLSX","text":"<p>Sintoma</p> <p>Intentas leer un archivo <code>xlsx</code> sin indicar el nombre de hoja.</p> <p>Causa El reader de Excel exige <code>sheet_name</code>.</p> <p>Solucion </p> Lectura correcta de XLSX<pre><code>datatable = DataTableCreator.from_file(\"people.xlsx\", sheet_name=\"People\")\n</code></pre>"},{"location":"troubleshooting/common-errors/#hoja-inexistente-en-xlsx","title":"Hoja inexistente en XLSX","text":"<p>Sintoma</p> <p>El archivo existe, pero el nombre de hoja no coincide.</p> <p>Causa La hoja pedida no aparece en el workbook.</p> <p>Solucion Valida el nombre exacto en Excel y vuelve a ejecutar con ese valor.</p>"},{"location":"troubleshooting/common-errors/#datos-no-rectangulares-en-from_records","title":"Datos no rectangulares en <code>from_records</code>","text":"<p>Sintoma</p> <p><code>from_records</code> falla al crear la tabla.</p> <p>Causa Al menos una fila no coincide con el esquema del primer registro.</p> ValidoInvalido Todas las filas comparten schema<pre><code>[\n    {\"name\": \"Alice\", \"age\": 30},\n    {\"age\": 25, \"name\": \"Bob\"},\n]\n</code></pre> Schema inconsistente<pre><code>[\n    {\"name\": \"Alice\", \"age\": 30},\n    {\"name\": \"Bob\", \"country\": \"MX\"},\n]\n</code></pre> <p>Solucion Normaliza el conjunto antes de construir el <code>DataTable</code> o agrega las columnas nuevas mediante mutacion controlada sobre la tabla ya creada.</p>"},{"location":"troubleshooting/common-errors/#ruta-inexistente","title":"Ruta inexistente","text":"<p>Sintoma</p> <p>La carga falla aunque el formato es correcto.</p> <p>Causa El archivo no existe en la ruta entregada.</p> <p>Solucion Revisa ruta absoluta o relativa, directorio de ejecucion y permisos del proceso.</p>"},{"location":"troubleshooting/common-errors/#diferencia-de-tipos-al-leer-csv","title":"Diferencia de tipos al leer CSV","text":"<p>Sintoma</p> <p>Un valor numerico llega como texto despues de leer <code>CSV</code>.</p> <p>Causa <code>CSV</code> no preserva tipos nativos de la misma manera que <code>JSON</code> o <code>XLSX</code>.</p> <p>Solucion Si el flujo depende de tipos, usa <code>JSON</code>, <code>XLSX</code> o crea la tabla con <code>from_records</code>.</p>"},{"location":"troubleshooting/common-errors/#acceso-a-columna-inexistente","title":"Acceso a columna inexistente","text":"<p>Sintoma</p> <p>Leer <code>datatable[0][\"country\"]</code> o <code>datatable[0].country</code> falla.</p> <p>Causa La columna no existe en el esquema actual.</p> <p>Solucion Agrega la columna primero con una mutacion valida:</p> Expandir el esquema<pre><code>datatable[0][\"country\"] = \"MX\"\n</code></pre> Regla util <p>Si agregas una columna en una fila, <code>pytabify</code> la propaga al resto con <code>None</code>. Ese es el camino esperado para enriquecer una tabla ya valida.</p>"}]}